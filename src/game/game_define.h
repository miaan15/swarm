#pragma once

#include "define.h"

// ACTION
// =============================================================================
enum {
    ACTION_NIL = 0,
};
constexpr u32 ACTION_EFFECT_OFFSET = 100;

// EFFECT
// =============================================================================
enum {
    EFFECT_NIL = 0,
};
